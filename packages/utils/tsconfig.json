{
  "extends": "@dao-dao/config/ts/react-library.json",
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    // Must be specified even when extending from a base config.
    "outDir": "dist"
  }
}
